{
  "name": "playnite-viewer",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "web/**",
    "worker/**"
  ],
  "scripts": {
    "up": "docker compose --progress=plain -f docker-compose.yml up --build -d"
  }
}