{
  "name": "interlinked",
  "private": true,
  "version": "1.40.6",
  "workspaces": [
    "web",
    "api",
    "scripts"
  ],
  "scripts": {
    "up": "node scripts/docker-up.mjs",
    "commit": "cz",
    "build": "npm run build:version && npm -ws --if-present run build && npm run build:ext",
    "push": "node scripts/git-push.mjs",
    "release": "node scripts/docker-release.mjs",
    "lint": "npm -ws --if-present run lint",
    "test": "npm -ws --if-present run test",
    "e2e:up": "docker compose -f docker-compose.yml -f docker-compose.e2e.yml up -d web api e2e-report",
    "e2e": "docker compose -f docker-compose.yml -f docker-compose.e2e.yml run --rm e2e",
    "e2e:gate": "node scripts/e2e-gate.js",
    "build:ext": "node scripts/build-extension.js",
    "build:version": "node scripts/set-version.mjs",
    "clean": "node scripts/util-clean.js",
    "codebase": "node scripts/util-codebase.mjs",
    "changes": "node scripts/util-changes.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.19.13",
    "cz-conventional-changelog": "^3.3.0",
    "standard-version": "^9.5.0"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
